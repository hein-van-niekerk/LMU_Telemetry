This is a passion project for a sim racing title called Lemans Ultimate. Massively still a work in progress.

This app makes use of duckdb files in order to interpret telemetry from the in-game data channels (these data channels are saved in the duckdb file).
Upon launch, a user must upload their duckdb file. This will allow them to see their data such as where on track they are applying inputs, this will allow for analysis of performance. 
Further plans are to incorporate track maps for data to be laid over on, as well as clean up the UI. 

Track maps were attempted to be generated in a Motec style; I would record several laps, convert the GPS points to meters, average the paths to get a smooth centerline spline, and then express every telemetry point as distance along that centerline plus sideways offset instead of trying to match it to a picture of the track. This proved to be too unreliable.
Another method is planned for implementation.

Far future plans include:
Agentic coaching + car setup assistance/guidance.
Database implementation for storing long term data instead of relying on duckdb interpretation.

---

## Experimental: OSM/Telem Corridor Track Map Generation

> **Branch:** `experimental/osm-telem-corridor`

Track maps are now generated by aligning OpenStreetMap (OSM) circuit geometry to the game's internal telemetry coordinate system, replacing the unreliable multi-lap averaging approach that was attempted previously.

### What it does

When you open a telemetry file for a track that has no pre-built map, the app automatically:

1. Looks up the OSM reference polyline for that track (stored in `LMU_Telemetry/Reference/`)
2. Finds the rotation, scale, and translation that aligns the OSM geometry to the game's GPS coordinate frame (Kabsch/Procrustes similarity transform with automatic arc-offset detection)
3. Resamples the aligned centreline to 3 m spacing and computes heading and curvature at every point
4. Bins the tyre-track GPS points laterally to estimate left/right track width
5. Saves the result to `LMU_Telemetry/TrackMaps/{TrackName}.json` for all future sessions

No button click or manual step is needed — it runs in the background on first load and is ready by the time you start reviewing your data.

### Tracks with OSM reference data

17 circuits are included out of the box:

| Track | OSM file |
|---|---|
| Circuit de Spa-Francorchamps | `spa_osm_stitched.json` ✓ verified |
| Autodromo Nazionale Monza | `monza_stitched.json` |
| Silverstone Circuit | `silverstone_stitched.json` |
| Circuit de Barcelona-Catalunya | `barcelona_catalunya_stitched.json` |
| Bahrain International Circuit (endurance) | `bahrain_endurance_stitched.json` |
| Bahrain International Circuit (outer) | `bahrain_outer_stitched.json` |
| Circuit of The Americas | `cota_stitched.json` |
| Daytona International Speedway | `daytona_road_course_stitched.json` |
| Fuji Speedway | `fuji_stitched.json` |
| Autodromo Enzo e Dino Ferrari (Imola) | `imola_stitched.json` |
| Autodromo Jose Carlos Pace (Interlagos) | `interlagos_stitched.json` |
| WeatherTech Raceway Laguna Seca | `lagunaseca_stitched.json` |
| Circuit Bugatti (Le Mans) | `lemans_bugatti_stitched.json` |
| Lusail International Circuit | `lusail_stitched.json` |
| Circuit Paul Ricard | `paulricard_stitched.json` |
| Autodromo Internacional do Algarve (Portimão) | `portimao_stitched.json` |
| Sebring International Raceway | `sebring_stitched.json` |

### Adding a new track

If the app auto-generation fails for a track (logged as "no OSM mapping found"), the exact in-game track name will appear in the log. Add it to `LMU_Telemetry/scripts/track_registry.py`:

```python
"Exact In-Game Track Name": "your_osm_stitched.json",
```

Then add the OSM JSON to `LMU_Telemetry/Reference/` and the next time telemetry for that track is opened, the map will generate automatically.

See [`LMU_Telemetry/scripts/README.md`](LMU_Telemetry/scripts/README.md) for full technical detail on the pipeline.


UI so far:
<img width="1920" height="1032" alt="image" src="https://github.com/user-attachments/assets/993ce559-ae36-41c3-8560-d342479cddfd" />
